//!====================================== CLASS ====================================//

//*In JavaScript, a class is a special syntax used for creating objects and implementing object-oriented programming concepts. It provides a clear and structured way to define objects, along with their properties and methods. Classes make it easier to write reusable, organized, and scalable code.

// class Player {
//     constructor(name, score) {
//         this.name = name;
//         this.score = score;
//     }

//     showScore() {
//         console.log(`${this.name} has a score of ${this.score}.`);
//     }

//     compare(otherPlayer) {
//         if (this.score > otherPlayer.score) {
//             console.log(`${this.name} has a higher score than ${otherPlayer.name}.`);
//         } else if (this.score < otherPlayer.score) {
//             console.log(`${otherPlayer.name} has a higher score than ${this.name}.`);
//         } else {
//         console.log(`${this.name} and ${otherPlayer.name} have the same score.`);
//         }
//     }
// }

// Create two objects
// const player1 = new Player("John", 80);
// const player2 = new Player("Jane", 90);

//   // Call methods for both objects
//   player1.showScore(); // Output: John has a score of 80.
//   player2.showScore(); // Output: Jane has a score of 90.

//   player1.compare(player2); // Output: Jane has a higher score than John.



// Example 2nd



// class Book {
//     constructor(title, author) {
//       this.title = title;
//       this.author = author;
//     }
  
//     describe() {
//       console.log(`"${this.title}" is written by ${this.author}.`);
//     }
//   }
  
//   // Create two objects
//   const book1 = new Book("1984", "George Orwell");
//   const book2 = new Book("To Kill a Mockingbird", "Harper Lee");
  
//   // Call methods for both objects
//   book1.describe(); // Output: "1984" is written by George Orwell.
//   book2.describe(); // Output: "To Kill a Mockingbird" is written by Harper Lee.
  

